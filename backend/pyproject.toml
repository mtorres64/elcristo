[tool.ruff]
line-length = 100
select = ["E", "F", "I", "UP", "B", "N"]
ignore = ["B008"]  # FastAPI Depends() en argumentos de función

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff.isort]
known-first-party = ["app"]
